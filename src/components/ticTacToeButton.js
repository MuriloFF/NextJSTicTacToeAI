import { Button } from '@material-ui/core'
import styles from '../styles/Home.module.css'

export default function TicTacToeButton({ ...props }) {
    return <Button  {...props} variant="contained" >
    </Button>
}
